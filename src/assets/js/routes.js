const routes = {
    GET: {
        root: '/',
        getSongs: '/getSongs'
    },
    POST: {
        uploadSong: '/uploadSong',
        uploadComplete: '/uploadComplete'
    }
}

module.exports = routes;